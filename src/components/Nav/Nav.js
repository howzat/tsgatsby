import React from "react"
import {Link} from "gatsby"

const SiteNavigation = (props) => {
	return (
			<header>
				<Link to="/" className={"text-blue"}>Toldspaces Navigation for the win</Link>
			</header>
	)
}

export default SiteNavigation